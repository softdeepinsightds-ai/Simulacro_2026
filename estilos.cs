using System;
using System.Drawing; // Assuming WinForms or similar; adjust for WPF if needed

public static class Estilos
{
    // Colores principales
    public static readonly Color ColorPrimario = Color.Blue;
    public static readonly Color ColorSecundario = Color.Gray;
    public static readonly Color ColorFondo = Color.White;

    // Fuentes
    public static readonly Font FuentePrincipal = new Font("Arial", 12, FontStyle.Regular);
    public static readonly Font FuenteTitulo = new Font("Arial", 16, FontStyle.Bold);

    // Otros estilos (ejemplo: márgenes)
    public const int MargenEstándar = 10;
}